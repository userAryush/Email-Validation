# Email-Validation in Python
The program checks if the entered email is valid or not.

## Features

- Ensures first character is an alphabet
- Checks minimum length (6 characters)
- Disallows uppercase letters
- Disallows spaces
- Requires exactly one '@' symbol
- Ensures email ends with `.com`



